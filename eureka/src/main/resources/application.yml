eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false  #关闭自我保护，开发环境可以关掉,生产环境不可以
server:
  port: 8761
spring:
  application:
    name: eureka